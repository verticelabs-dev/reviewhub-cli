{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Orchestrator",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/orchestrator"
        }
    ]
}